import React from 'react'
import { AppBar, Toolbar, ListItem, IconButton, 
    ListItemText, Avatar, Divider, 
    List, Typography, Box } from '@material-ui/core';
import {ArrowBack,AssignmentId} from '@material-ui/core/Icon'; 

const Navbar = () => {
    return (
        <Box component="nav">
            <AppBar>

            </AppBar>

        </Box>
    )
}

export default Navbar
